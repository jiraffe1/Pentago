//TODO: rank every space by how many non-empty pieces it neighbours and pick the one with the most. then make a random rotation? or maybe include rotations with the heuristic.

function makeHuddleMove() {
  makeRandomMove();
}